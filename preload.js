// This file runs before the renderer process (Next.js) is loaded.
// It has access to Node.js APIs and the DOM.

window.addEventListener('DOMContentLoaded', () => {
    console.log('Electron preload script loaded.');
});