rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all operations for seeding (remove after seeding)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}